import type { Address } from './common';

/** Mock account for the playground */
export type Account = {
  id: string;
  address: Address;
  name: string;
  avatarColor: string;
  balance: string;
};
